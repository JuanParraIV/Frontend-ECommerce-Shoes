import tw from 'twin.macro';
import styled from 'styled-components';

/* export const ProductsHeading = styled.div`${tw`text-center my-10 mx-0`}
  h2{
    ${tw`text-4xl`}
  }
  p{
    ${tw`text-base font-extrabold`}
  }
  span{
    ${tw`text-yellow-400`}
  }`; */
  export const Copy = styled.div`${tw``}`;
export const flexBetween = "flex items-center";

export const NavBarHeader = styled.header`${tw`sticky top-0 z-50  shadow-md`}`;

export const NavBarContainer = styled.div`${tw`${flexBetween} flex-grow p-1 py-2 bg-gray-700`}`;
export const ImageLogoContainer = styled.div`${tw`${flexBetween} mt-2 flex-grow sm:flex-grow-0`}`;

